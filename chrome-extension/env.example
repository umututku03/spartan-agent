# Google Meet Plugin Configuration
# Chrome Extension-based implementation

# Extension WebSocket Configuration
# Port for WebSocket server (default: 8765)
EXTENSION_WS_PORT=8765

# OpenAI Configuration (for transcription)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Transcription Settings
# Language for transcription (default: en)
# Supports: en, es, fr, de, it, pt, ru, ja, ko, zh, etc.
TRANSCRIPTION_LANGUAGE=en

# Audio chunk duration in milliseconds (default: 30000 = 30 seconds)
# Lower values = more frequent transcription, higher cost
AUDIO_CHUNK_DURATION_MS=30000

# Enable real-time transcription (default: true)
ENABLE_REAL_TIME_TRANSCRIPTION=true

# Report generation settings
# Directory for saving meeting reports
REPORT_OUTPUT_DIR=./meeting-reports 